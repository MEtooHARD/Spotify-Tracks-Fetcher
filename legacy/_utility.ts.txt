import config from './config.json';
import { Cred } from './types';

export const creds: Cred[] = config.spotify
    .map(c => ({
        secret: c.secret,
        clientID: c.clientID,
        rate_limit: {
            limited: false,
            target_time: 0
        }
    }));


