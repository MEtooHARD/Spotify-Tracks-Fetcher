// import { Cred } from "../api/token_manager";
import config from '../config.json' with {type: 'json'};

export const Config = config;

// export const creds: Cred[] = Config.spotify.map(s => ({
//     clientID: s.clientID,
//     secret: s.secret
// }))

